import {useState} from 'react'

const pickupDate = ({pickupDateProp}) => {
  const [state, setState] = useState({
    pickupDateState: pickupDateProp,
  })

  const pickupDateState = state
    
}

export default pickupDate